This project is a University Database Management System built using MySQL.
It demonstrates how to design relational tables, define relationships using foreign keys, and perform advanced SQL queries such as joins, subqueries, aggregations, and analytics.

The database models a simple academic environment with students, professors, courses, and exam marks.
